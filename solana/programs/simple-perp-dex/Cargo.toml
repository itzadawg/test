[package]
name = "simple-perp-dex"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "simple_perp_dex"

[features]
default = []
no-entrypoint = []
cpi = ["no-entrypoint"]
idl-build = []

[dependencies]
anchor-lang = { workspace = true }
anchor-spl = { workspace = true }
